# Ubuntu/Debian-only packages — different name or install source vs Arch
# See packages/common.txt for packages shared with Arch

# github CLI (Ubuntu/Debian package name; Arch uses 'github-cli')
gh

# neovim: downloaded from GitHub releases in install.sh (apt version is outdated)
# rustup: installed via rustup.rs script in install.sh (not in apt repos)
